# Crosses & Clippers — Two Image Update

This update makes only the two requested image changes:

1. Hero section: `assets/img/hero-davian.jpg`
2. Meet Davian section: `assets/img/meet-davian.jpg`

Replace the repository's existing `index.html` with the included `index.html`, and copy the included `assets/img/` files into the existing `assets/img/` directory.

No changes are required to `style.css`, `app.js`, Supabase files, or any other existing site structure.
